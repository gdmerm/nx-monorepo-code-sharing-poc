import { dataAccessCategories } from './data-access-categories';

describe('dataAccessCategories', () => {
  it('should work', () => {
    expect(dataAccessCategories()).toEqual('data-access-categories');
  });
});
